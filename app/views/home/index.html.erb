<!DOCTYPE html>
<html>
<head>
  <title>Food Donation - Help End Hunger</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application" %>

  <style>
    :root {
      --primary: #2E8B57;       /* Sea Green - earthy, natural */
      --primary-dark: #1F6540;  /* Darker green for hover states */
      --secondary: #F9A826;     /* Golden/Orange - warm, inviting */
      --accent: #F05454;        /* Coral red for call-to-actions */
      --light: #F8F9FA;         /* Off-white for backgrounds */
      --dark: #2D3142;          /* Dark blue-gray for text */
      --white: #FFFFFF;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Montserrat', 'Arial', sans-serif;
      background: var(--light);
      color: var(--dark);
      line-height: 1.6;
    }

    .container {
      width: 90%;
      max-width: 1200px;
      margin: 0 auto;
    }

   

   

    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 24px;
      font-weight: 700;
      color: var(--primary);
    }

    .logo-icon {
      font-size: 28px;
      color: var(--primary);
    }

    .nav-links {
      display: flex;
      gap: 30px;
    }

    .nav-link {
      text-decoration: none;
      color: var(--dark);
      font-weight: 500;
      transition: color 0.3s;
    }

    .nav-link:hover {
      color: var(--primary);
    }

    .nav-link.active {
      color: var(--primary);
      font-weight: 600;
    }

    /* Hero Section */
    .hero {
      background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('https://source.unsplash.com/1600x900/?food,donation') center/cover no-repeat;
      color: var(--white);
      text-align: center;
      padding: 160px 20px;
      position: relative;
    }

    .hero-content {
      max-width: 800px;
      margin: 0 auto;
    }

    .hero h1 {
      font-size: 54px;
      font-weight: 800;
      margin-bottom: 20px;
      line-height: 1.2;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
    }

    .hero p {
      font-size: 22px;
      margin-bottom: 40px;
      text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
    }

    .btn {
      display: inline-block;
      padding: 15px 35px;
      font-size: 18px;
      border-radius: 50px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s;
      text-transform: uppercase;
      letter-spacing: 1px;
      border: none;
      cursor: pointer;
    }

    .btn-primary {
      background: var(--accent);
      color: var(--white);
      box-shadow: 0 4px 15px rgba(240, 84, 84, 0.3);
    }

    .btn-primary:hover {
      background: #e63c3c;
      transform: translateY(-3px);
      box-shadow: 0 6px 20px rgba(240, 84, 84, 0.4);
    }

    /* Sections */
    .section {
      padding: 100px 20px;
      text-align: center;
    }

    .section-header {
      margin-bottom: 70px;
    }

    .section h2 {
      font-size: 42px;
      margin-bottom: 20px;
      color: var(--primary);
      position: relative;
      display: inline-block;
    }

    .section h2::after {
      content: '';
      width: 70px;
      height: 4px;
      background: var(--secondary);
      position: absolute;
      bottom: -15px;
      left: 50%;
      transform: translateX(-50%);
      border-radius: 2px;
    }

    .section-desc {
      font-size: 20px;
      color: #636E72;
      max-width: 700px;
      margin: 0 auto 40px;
    }

    /* Features */
    .features {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 30px;
    }

    .feature-box {
      width: 330px;
      background: var(--white);
      padding: 40px 30px;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
      transition: transform 0.3s, box-shadow 0.3s;
      position: relative;
      overflow: hidden;
    }

    .feature-box::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 5px;
      height: 100%;
      background: var(--primary);
      border-radius: 5px 0 0 5px;
    }

    .feature-box:hover {
      transform: translateY(-10px);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.12);
    }

    .feature-icon {
      font-size: 48px;
      color: var(--primary);
      margin-bottom: 25px;
    }

    .feature-box h3 {
      color: var(--dark);
      font-size: 24px;
      margin-bottom: 15px;
      font-weight: 600;
    }

    .feature-box p {
      color: #636E72;
      font-size: 16px;
      line-height: 1.7;
    }

    /* How It Works Section */
    .how-it-works {
      background: linear-gradient(135deg, #f9f9f9 0%, #f0f6ff 100%);
    }

    .steps {
      position: relative;
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 30px;
    }

    .steps::before {
      content: '';
      position: absolute;
      top: 70px;
      left: 50%;
      transform: translateX(-50%);
      width: 80%;
      height: 3px;
      background: #e1e8f0;
      z-index: 0;
    }

    .step-box {
      width: 300px;
      background: var(--white);
      padding: 40px 30px;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
      position: relative;
      z-index: 1;
    }

    .step-number {
      width: 60px;
      height: 60px;
      background: var(--primary);
      color: var(--white);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
      font-weight: 700;
      margin: -60px auto 20px;
      border: 5px solid var(--white);
      box-shadow: 0 5px 15px rgba(46, 139, 87, 0.3);
    }

    .step-box h3 {
      font-size: 22px;
      color: var(--primary);
      margin-bottom: 15px;
    }

    /* Call to Action */
    .cta {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
      color: var(--white);
    }

    .cta h2 {
      color: var(--white);
    }

    .cta h2::after {
      background: var(--secondary);
    }

    .cta-desc {
      color: rgba(255, 255, 255, 0.9);
      margin-bottom: 40px;
    }

    .btn-secondary {
      background: var(--white);
      color: var(--primary);
    }

    .btn-secondary:hover {
      background: var(--light);
      transform: translateY(-3px);
      box-shadow: 0 6px 20px rgba(255, 255, 255, 0.2);
    }

    /* Impact Stats */
    .impact-stats {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 40px;
      margin-top: 60px;
    }

    .stat-item {
      text-align: center;
      width: 220px;
    }

    .stat-number {
      font-size: 48px;
      font-weight: 700;
      color: var(--white);
      margin-bottom: 10px;
    }

    .stat-label {
      font-size: 18px;
      color: rgba(255, 255, 255, 0.9);
    }

    /* Footer */
    .footer {
      background: var(--dark);
      color: var(--white);
      padding: 60px 20px 30px;
      text-align: center;
    }

    .footer-content {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .footer-logo {
      font-size: 28px;
      font-weight: 700;
      color: var(--white);
      margin-bottom: 30px;
    }

    .footer-links {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 30px;
      margin-bottom: 40px;
    }

    .footer-link {
      color: rgba(255, 255, 255, 0.8);
      text-decoration: none;
      transition: color 0.3s;
    }

    .footer-link:hover {
      color: var(--white);
    }

    .social-links {
      display: flex;
      gap: 20px;
      margin-bottom: 40px;
    }

    .social-icon {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
      transition: background 0.3s;
    }

    .social-icon:hover {
      background: var(--primary);
    }

    .copyright {
      color: rgba(255, 255, 255, 0.6);
      font-size: 14px;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      padding-top: 20px;
      width: 100%;
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
      .hero h1 {
        font-size: 40px;
      }
      
      .hero p {
        font-size: 18px;
      }
      
      .section h2 {
        font-size: 32px;
      }
      
      .feature-box {
        width: 100%;
      }
      
      .steps::before {
        display: none;
      }
    }
  </style>
  
  <!-- Font Awesome for icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Google Fonts -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&display=swap">
</head>
<body>

  <!-- Navbar - maintaining original white color -->

  
  <!-- Hero Section -->
  <div class="hero">
    <div class="hero-content">
      <h1>Help End Hunger, One Meal at a Time</h1>
      <p>Your contribution can feed those in need. Join our mission to create a hunger-free world.</p>
      <h1>Donate Now</h1>
    </div>
  </div>

  <!-- Why Food Donation Matters -->
  <div class="section">
    <div class="container">
      <div class="section-header">
        <h2>Why Food Donation Matters</h2>
        <p class="section-desc">Millions of people go to bed hungry every night. Your small act of kindness can make a significant difference in someone's life.</p>
      </div>
      <div class="features">
        <div class="feature-box">
          <i class="fas fa-recycle feature-icon"></i>
          <h3>Reduce Food Waste</h3>
          <p>Each year, millions of tons of food go to waste while people go hungry. By donating food, you help break this cycle and create a more sustainable world.</p>
        </div>
        <div class="feature-box">
          <i class="fas fa-hands-helping feature-icon"></i>
          <h3>Support Communities</h3>
          <p>Your donations help provide nutritious meals to underprivileged families, children, elderly, and homeless individuals in our local communities.</p>
        </div>
        <div class="feature-box">
          <i class="fas fa-heart feature-icon"></i>
          <h3>Make a Social Impact</h3>
          <p>Be part of a larger mission to eradicate hunger. Every donation, no matter how small, contributes to meaningful social change for generations to come.</p>
        </div>
      </div>
    </div>
  </div>

  <!-- How It Works -->
  <div class="section how-it-works">
    <div class="container">
      <div class="section-header">
        <h2>How It Works</h2>
        <p class="section-desc">Donating food is simple and quick. Follow these three easy steps to make a difference today:</p>
      </div>
      <div class="steps">
        <div class="step-box">
          <div class="step-number">1</div>
          <h3>Register</h3>
          <p>Create an account and let us know what and how much food you want to donate. Both individuals and businesses are welcome.</p>
        </div>
        <div class="step-box">
          <div class="step-number">2</div>
          <h3>Schedule</h3>
          <p>Choose between convenient pickup from your location or dropping off your donation at one of our partner centers.</p>
        </div>
        <div class="step-box">
          <div class="step-number">3</div>
          <h3>Help a Family</h3>
          <p>Your food goes directly to those who need it most. We'll even share the impact your donation has made in our community.</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Call to Action -->
  <div class="section cta">
    <div class="container">
      <h2>Be a Part of the Change</h2>
      <p class="section-desc cta-desc">Join our growing community of donors and volunteers who are making the world a better place one meal at a time.</p>
      <%# <a href="/donate" class="btn btn-secondary">Start Donating</a> %>
      
      <div class="impact-stats">
        <div class="stat-item">
          <div class="stat-number">12K+</div>
          <div class="stat-label">Meals Donated</div>
        </div>
        <div class="stat-item">
          <div class="stat-number">3K+</div>
          <div class="stat-label">Families Helped</div>
        </div>
        <div class="stat-item">
          <div class="stat-number">500+</div>
          <div class="stat-label">Active Donors</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="footer">
    <div class="container footer-content">
      <div class="footer-logo">
        <i class="fas fa-seedling"></i> FoodShare
      </div>
      <div class="footer-links">
        <a href="#" class="footer-link">About Us</a>
        <a href="#" class="footer-link">How to Donate</a>
        <a href="#" class="footer-link">Where We Work</a>
        <a href="#" class="footer-link">Volunteer</a>
        <a href="#" class="footer-link">Contact Us</a>
      </div>
      <div class="social-links">
        <a href="#" class="social-icon"><i class="fab fa-facebook-f"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-twitter"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-instagram"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-linkedin-in"></i></a>
      </div>
      <div class="copyright">
        <p>© 2025 FoodShare | Making a Difference Together</p>
      </div>
    </div>
  </footer>

</body>
</html>