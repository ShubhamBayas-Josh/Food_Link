<p style="color: green"><%= notice %></p>

<% content_for :title, "Admin Details" %>

<h1>Admin Details</h1>

<div class="card">
  <div class="card-body">
    <p><strong>Name:</strong> <%= @admin.name %></p>
  </div>
</div>

<div class="mt-3">
  <%# <%= link_to "Edit", edit_admin_path(@admin), class: "btn btn-warning" %> 
  <%= link_to "Back to admins", admins_path, class: "btn btn-secondary" %>
  <%# <%= button_to "Delete", @admin, method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure?" } %> 
</div>
